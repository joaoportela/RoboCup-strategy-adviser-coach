This is the statistics calculator module. It uses a java program based on the
SoccerScope2 to build the statistics from the logs and generate a xml file.
Then the python scripts can be used to transform that data into csv (for
a spreadsheets and stuff).
